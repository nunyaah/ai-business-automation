FROM python:3.11-slim

WORKDIR /app

# Copy the whole repo so demo can reach agent sample data
COPY . .

WORKDIR /app/demo
RUN pip install --no-cache-dir -r requirements.txt

EXPOSE 8080
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8080"]
